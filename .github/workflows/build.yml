name: CI

on: push

jobs:
  test:
    name: Run tests
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Install Python deps & ZCompose
      run: pip install . pytest

    - name: Run tests
      run: pytest tests/